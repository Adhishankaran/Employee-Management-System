-- Create Employee table
CREATE TABLE Employees (
    employee_id INT PRIMARY KEY AUTO_INCREMENT,
    employee_name VARCHAR(100) NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    phone_number VARCHAR(15) NOT NULL
);

-- Create Task table
CREATE TABLE Tasks (
    task_id INT PRIMARY KEY AUTO_INCREMENT,
    task_name VARCHAR(150) NOT NULL,
    task_description TEXT,
    assigned_employee_id INT,
    FOREIGN KEY (assigned_employee_id) REFERENCES Employees(employee_id)
        ON DELETE SET NULL
        ON UPDATE CASCADE
);


INSERT INTO Employees (employee_name, email, phone_number)
VALUES ('John Doe', 'john@example.com', '9876543210');

-- Update Employee Details
UPDATE Employees
SET employee_name = 'John Smith',
    email = 'johnsmith@example.com',
    phone_number = '9123456789'
WHERE employee_id = 1;

-- Delete Employee
DELETE FROM Employees
WHERE employee_id = 1;

-- Assign Task to Employee
INSERT INTO Tasks (task_name, task_description, assigned_employee_id)
VALUES ('Prepare Report', 'Prepare monthly sales report', 2);

-- View Employees with Assigned Tasks
SELECT e.employee_id,
       e.employee_name,
       e.email,
       e.phone_number,
       t.task_id,
       t.task_name,
       t.task_description
FROM Employees e
LEFT JOIN Tasks t
ON e.employee_id = t.assigned_employee_id;